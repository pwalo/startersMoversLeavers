extends layout

block content
    if user
      h1 #{title}
      ul.list-group
        each option, i in options
          li.list-group-item 
            a(href="/options/edit/"+option._id)= option.name
            p=option.description
    else
      h3 Administrators Only
      p Contact RIT if you need to add a system user
          