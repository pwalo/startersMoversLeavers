extends layout

block content
    if user
      h1 #{title}
      ul.list-group
        each site, i in sites
          li.list-group-item 
            a(href="/site/edit/"+site._id)= site.code+' : '+site.site
          br
    else
      h3 Administrators Only
      p Contact RIT if you need to add a system user
