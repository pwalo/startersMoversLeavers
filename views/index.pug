extends layout

block content
    h1 Create-Read-Update-Delete
    h2 Employees, equipment, roles etc...
    h3 #{title}
    ul.list-group
      each option, i in options
        li.list-group-item 
          a(href="/"+option.url)= option.name
          p=option.description
          