extends layout

block content
    h1 #{title}
    ul.list-group
      each company, i in companys
        li.list-group-item 
          a(href="/company/edit/"+company._id)= company.code+' : '+company.company
        br
