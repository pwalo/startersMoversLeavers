extends layout

block content
    if user
      h1 #{title}
      ul.list-group
        each company, i in companys
          li.list-group-item 
            a(href="/company/edit/"+company._id)= company.code+' : '+company.company
          br
    else
      h3 Administrators Only
      p Contact RIT if you need to add a system user
